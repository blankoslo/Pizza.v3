import withSvgr from 'next-svgr'

/**
 * @type {import('next').NextConfig}
 * @type {import('next-svgr').withSvgr}
 */

const nextConfig = { reactStrictMode: true }

export default withSvgr(nextConfig)
